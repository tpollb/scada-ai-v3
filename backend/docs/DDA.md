# Deep Data Analysis (DDA)

**Версия:** 3.2.8  
**Последнее обновление:** 2026-06-30  

---

## Содержание

1. Введение
2. Архитектура модуля
3. Математические основы
4. Конфигуратор параметров
5. API Reference
6. Примеры использования
7. Технические детали
8. Сезонный анализ (Seasonal Analysis)

---

## Введение

Модуль **Deep Data Analysis (DDA)** предназначен для комплексного анализа временных рядов данных SCADA-систем. Модуль предоставляет:

- **Детекцию аномалий** с использованием алгоритма Isolation Forest
- **Классификацию аномалий** по типам: пики (spike), провалы (dip), дрейфы (drift), шум (noise)
- **Корреляционный анализ** между несколькими тегами
- **Визуализацию** с интерактивными графиками и тепловой картой корреляций
- **Конфигурируемость** через UI-интерфейс настроек

### Ключевые возможности

- [x] Анализ одиночных тегов и множественных тегов
- [x] Автоматическая классификация типов аномалий
- [x] Интерактивные графики с zoom/pan
- [x] Настраиваемые пороги детекции
- [x] Корреляционный анализ с визуализацией
- [x] Экспорт результатов в PNG

---

## Архитектура модуля

### Структура файлов

```
backend/modules/deep_analysis/
├── __init__.py
├── api.py                          # API endpoints
├── analyzers/
│   ├── __init__.py
│   ├── anomalies.py               # Isolation Forest + классификация
│   ├── correlations.py            # Матрица корреляций
│   └── stats.py                   # Базовая статистика
├── collectors/
│   ├── __init__.py
│   ├── data_fetcher.py            # Получение данных из БД
│   └── tag_resolver.py            # Резолвер тегов
├── history/
│   ├── __init__.py
│   └── storage.py                 # Хранение истории анализов
└── visualizers/
    ├── __init__.py
    └── chart_specs.py             # Генерация Chart.js спецификаций

frontend/src/components/
├── DeepAnalysisResults.svelte     # Основной компонент результатов
├── ChartModal.svelte              # Полноэкранный просмотр графиков
└── DDAConfigModal.svelte          # Конфигуратор настроек
```

### Поток данных

```
1. Пользователь выбирает теги и период анализа
   ↓
2. API получает данные из БД (data_fetcher.py)
   ↓
3. Данные выравниваются по общей временной сетке
   ↓
4. Применяется Isolation Forest для детекции аномалий
   ↓
5. Аномалии классифицируются по типам
   ↓
6. Вычисляются корреляции между тегами
   ↓
7. Генерируются спецификации графиков (Chart.js)
   ↓
8. Результаты отображаются в UI
```

---

## Математические основы

### 1. Isolation Forest

**Isolation Forest** — алгоритм машинного обучения для детекции аномалий, основанный на принципе "изоляции".

#### Основная идея

Аномальные точки легче "изолировать" (отделить от остальных) чем нормальные точки. Алгоритм строит набор случайных деревьев решений (isolation trees) и измеряет среднюю глубину изоляции каждой точки.

#### Алгоритм построения Isolation Tree

Для каждого дерева:

1. **Инициализация:** выбираем подвыборку размера `ψ` (обычно 256 точек)
2. **Рекурсивное разделение:**
   - Выбираем случайный признак (в нашем случае — индекс точки)
   - Выбираем случайное значение разделения `split_value` между min и max
   - Разделяем данные на две части: `x < split_value` и `x >= split_value`
3. **Остановка:** когда достигнута максимальная глубина или все точки разделены

#### Математическая формула

**Anomaly Score** для точки `x`:

```
s(x, n) = 2^(-E(h(x)) / c(n))
```

Где:
- `E(h(x))` — средняя глубина изоляции точки `x` по всем деревьям
- `c(n)` — нормализующий фактор (средняя глубина в бинарном дереве поиска)

**Нормализующий фактор:**

```
c(n) = 2H(n-1) - 2(n-1)/n

H(i) ≈ ln(i) + 0.5772156649 (гармоническое число)
```

#### Интерпретация результатов

- `s(x, n) → 1`: точка является аномалией (легко изолируется)
- `s(x, n) → 0.5`: точка нормальная
- `s(x, n) → 0`: точка является аномалией (но в другом смысле)

#### Параметры алгоритма

- **contamination** (загрязнение): доля аномалий в данных (0.01 - 0.5)
- **n_estimators**: количество деревьев (обычно 100)
- **max_samples**: размер подвыборки для каждого дерева

### 2. Классификация аномалий

После детекции аномалий алгоритм классифицирует их по типам на основе локального контекста.

#### Методология

Для каждой аномальной точки анализируется окно `W` (обычно ±5 точек):

```
W = [x(t-w), ..., x(t-1), x(t), x(t+1), ..., x(t+w)]
```

#### Типы аномалий

##### Spike (Пик)

**Определение:** резкое отклонение вверх от локального тренда

**Критерии:**
```
spike = (x(t) > mean(W \ {x(t)}) + 2σ) AND 
        (|x(t) - x(t-1)| > threshold) AND
        (|x(t) - x(t+1)| > threshold)
```

**Физический смысл:** кратковременный всплеск значения (например, помеха датчика)

##### Dip (Провал)

**Определение:** резкое отклонение вниз от локального тренда

**Критерии:**
```
dip = (x(t) < mean(W \ {x(t)}) - 2σ) AND 
      (|x(t) - x(t-1)| > threshold) AND
      (|x(t) - x(t+1)| > threshold)
```

**Физический смысл:** кратковременное падение значения (например, обрыв сигнала)

##### Drift (Дрейф)

**Определение:** постепенное отклонение от базового уровня

**Критерии:**
```
drift = |x(t) - baseline(t)| > threshold

baseline(t) = moving_average(W) или экспоненциальное сглаживание
```

**Физический смысл:** медленное изменение значения (например, износ оборудования)

##### Noise (Шум)

**Определение:** аномалия, не попадающая под другие категории

**Критерии:**
```
noise = is_anomaly(x(t)) AND 
        NOT (spike OR dip OR drift)
```

**Физический смысл:** случайные флуктуации или неклассифицируемые аномалии

### 3. Корреляционный анализ

#### Коэффициент корреляции Пирсона

**Формула:**

```
r(X, Y) = Σ((xᵢ - x̄)(yᵢ - ȳ)) / √(Σ(xᵢ - x̄)² · Σ(yᵢ - ȳ)²)
```

Где:
- `X, Y` — два временных ряда
- `x̄, ȳ` — средние значения
- `r ∈ [-1, 1]`

#### Интерпретация

- `r = 1`: полная положительная корреляция
- `r = -1`: полная отрицательная корреляция
- `r = 0`: отсутствие линейной корреляции

#### Матрица корреляций

Для `n` тегов строится матрица `C` размером `n × n`:

```
C[i, j] = r(tagᵢ, tagⱼ)
```

**Свойства:**
- Симметричность: `C[i, j] = C[j, i]`
- Диагональ: `C[i, i] = 1` (корреляция с самим собой)

### 4. Downsampling (понижение частоты дискретизации)

#### Min-Max Downsampling

Для визуализации больших объёмов данных применяется min-max downsampling.

**Алгоритм:**

1. Разделяем данные на `N` bucket'ов (корзин)
2. Для каждого bucket находим:
   - `min_value` с его индексом `i_min`
   - `max_value` с его индексом `i_max`
3. Добавляем обе точки в хронологическом порядке

**Преимущества:**
- Сохраняет экстремумы (пики и провалы)
- Уменьшает количество точек в 2-3 раза
- Быстрый алгоритм O(n)

**Формула размера bucket:**

```
bucket_size = total_points / target_points
```

---

## Конфигуратор параметров

### Доступ к конфигуратору

Конфигуратор открывается через кнопку ⚙️ (настройки) в панели Deep Data Analysis.

### Вкладки конфигуратора

#### 1. Детекция аномалий

##### Contamination (Загрязнение)

**Описание:** Ожидаемая доля аномалий в данных

**Диапазон:** 0.01 - 0.5  
**По умолчанию:** 0.08 (8%)

**Рекомендации:**
- `0.01-0.05`: для чистых данных с редкими аномалиями
- `0.05-0.10`: для типичных производственных данных
- `0.10-0.20`: для зашумлённых данных
- `0.20-0.50`: для данных с большим количеством аномалий

**Влияние:**
- Увеличение → больше точек помечается как аномалии
- Уменьшение → более строгая фильтрация

##### N Estimators (Количество деревьев)

**Описание:** Количество деревьев в Isolation Forest

**Диапазон:** 50 - 500  
**По умолчанию:** 100

**Рекомендации:**
- `50-100`: быстрая детекция, хорошее качество
- `100-200`: баланс скорости и точности
- `200-500`: максимальная точность (медленнее)

**Влияние:**
- Увеличение → более стабильные результаты, но медленнее
- Уменьшение → быстрее, но больше вариативности

##### Классификация типов

**Описание:** Включить/выключить классификацию аномалий по типам

**Тип:** Чекбокс  
**По умолчанию:** [x] Включено

**Влияние:**
- [x] Включено: аномалии классифицируются (spike/dip/drift/noise)
- X Выключено: все аномалии помечаются как "noise"

#### 2. Окно анализа

##### Размер окна (Window Size)

**Описание:** Количество точек для анализа локального контекста

**Диапазон:** 3 - 21  
**По умолчанию:** 11 (±5 точек)

**Рекомендации:**
- `3-5`: для высокочастотных данных
- `7-11`: для типичных данных (5-минутный интервал)
- `15-21`: для низкочастотных данных

**Влияние:**
- Увеличение → более сглаженная классификация
- Уменьшение → более чувствительная к локальным изменениям

##### Порог для Spike/Dip (Spike Threshold)

**Описание:** Минимальное отклонение для классификации как spike/dip

**Диапазон:** 1.0 - 5.0  
**По умолчанию:** 2.0 (2σ)

**Рекомендации:**
- `1.0-1.5`: чувствительная детекция
- `2.0-3.0`: стандартная детекция
- `3.0-5.0`: строгая детекция

**Влияние:**
- Увеличение → меньше ложных срабатываний
- Уменьшение → больше аномалий классифицируется как spike/dip

#### 3. Downsampling

##### Максимальное количество точек (Max Points)

**Описание:** Максимальное количество точек для отображения на графике

**Диапазон:** 500 - 10000  
**По умолчанию:** 3000

**Рекомендации:**
- `500-1000`: для медленных браузеров
- `2000-5000`: оптимальный баланс
- `5000-10000`: для детального анализа (требует больше ресурсов)

**Влияние:**
- Увеличение → более детальный график, но медленнее рендеринг
- Уменьшение → быстрее рендеринг, но меньше деталей

##### Метод downsampling

**Описание:** Алгоритм понижения частоты дискретизации

**Тип:** Выпадающий список  
**Варианты:**
- `min-max`: сохраняет экстремумы (по умолчанию)
- `average`: усредняет значения в bucket'ах
- `first`: берёт первую точку каждого bucket'а

**Рекомендации:**
- `min-max`: для анализа аномалий (рекомендуется)
- `average`: для сглаженных данных
- `first`: для быстрого предпросмотра

#### 4. Корреляции

##### Минимальный порог корреляции (Min Correlation)

**Описание:** Минимальный коэффициент корреляции для отображения

**Диапазон:** 0.0 - 1.0  
**По умолчанию:** 0.3

**Рекомендации:**
- `0.0-0.2`: показывать все корреляции
- `0.3-0.5`: показывать значимые корреляции
- `0.5-1.0`: показывать только сильные корреляции

**Влияние:**
- Увеличение → меньше пар отображается в результатах
- Уменьшение → больше пар отображается

##### Автоматический выбор пар (Auto Select Pairs)

**Описание:** Автоматически выбирать пары с сильной корреляцией для scatter plot

**Тип:** Чекбокс  
**По умолчанию:** [x] Включено

**Влияние:**
- [x] Включено: автоматически строится scatter plot для самой сильной пары
- X Выключено: scatter plot не строится автоматически

---

## API Reference

### POST /api/v1/deep_analysis/run

Запускает анализ выбранных тегов.

#### Request Body

```json
{
  "tags": ["TAG1", "TAG2", ...],
  "period": "7d" | "30d" | "90d",
  "options": {
    "contamination": 0.08,
    "n_estimators": 100,
    "classify_types": true,
    "max_points": 3000,
    "min_correlation": 0.3
  }
}
```

#### Response

```json
{
  "success": true,
  "analysis_id": "uuid-v4",
  "results": {
    "anomalies": {
      "total": 42,
      "anomaly_indices": [10, 25, 100, ...],
      "anomaly_values": [45.2, 38.7, 52.1, ...],
      "anomaly_timestamps": ["2026-06-20 10:00", ...],
      "anomaly_types": ["spike", "dip", "drift", ...],
      "type_counts": {
        "spike": 15,
        "dip": 12,
        "drift": 8,
        "noise": 7
      }
    },
    "correlation_matrix": {
      "tags": ["TAG1", "TAG2"],
      "matrix": [[1.0, 0.85], [0.85, 1.0]]
    },
    "pair_analysis": {
      "tag1": "TAG1",
      "tag2": "TAG2",
      "pearson": {
        "coefficient": 0.85,
        "p_value": 0.001
      },
      "scatter_data": {
        "x": [10.5, 12.3, ...],
        "y": [11.2, 13.1, ...]
      }
    }
  },
  "visualizations": {
    "time_series": { ... },
    "heatmap": { ... },
    "scatter": { ... }
  }
}
```

### GET /api/v1/deep_analysis/history

Возвращает список сохранённых анализов.

#### Response

```json
{
  "analyses": [
    {
      "id": "uuid-v4",
      "tags": ["TAG1", "TAG2"],
      "period": "7d",
      "created_at": "2026-06-26T10:00:00Z",
      "total_anomalies": 42
    }
  ]
}
```

### GET /api/v1/deep_analysis/history/{id}

Возвращает полный результат сохранённого анализа.

---

## Примеры использования

### Пример 1: Анализ одиночного тега

**Сценарий:** Детекция аномалий в температуре реактора

**Шаги:**
1. Открыть панель Deep Data Analysis
2. Выбрать тег `REACTOR_TEMP`
3. Период: `7 дней`
4. Нажать "Анализировать"

**Ожидаемый результат:**
- График временного ряда с выделенными аномалиями
- Список аномалий с типами (spike/dip/drift/noise)
- Статистика: количество аномалий каждого типа

### Пример 2: Корреляционный анализ двух тегов

**Сценарий:** Проверка корреляции между температурой и давлением

**Шаги:**
1. Открыть панель Deep Data Analysis
2. Выбрать теги `REACTOR_TEMP` и `REACTOR_PRESSURE`
3. Период: `30 дней`
4. Нажать "Анализировать"

**Ожидаемый результат:**
- График временного ряда для обоих тегов
- Матрица корреляций (тепловая карта)
- Scatter plot с линией регрессии
- Коэффициент корреляции Пирсона

### Пример 3: Анализ множественных тегов

**Сценарий:** Комплексный анализ 5 связанных параметров

**Шаги:**
1. Открыть панель Deep Data Analysis
2. Выбрать теги: `TEMP1`, `TEMP2`, `PRESSURE`, `FLOW`, `VIBRATION`
3. Период: `7 дней`
4. Настроить параметры:
   - Contamination: `0.10`
   - Min Correlation: `0.5`
5. Нажать "Анализировать"

**Ожидаемый результат:**
- Multi-tag график с 5 линиями
- Тепловая карта корреляций 5×5
- Список всех аномалий для каждого тега
- Scatter plot для самой сильной коррелированной пары

---

## Технические детали

### Производительность

**Время анализа:**
- 1 тег, 7 дней (~10,000 точек): ~2-3 секунды
- 5 тегов, 7 дней (~50,000 точек): ~8-10 секунд
- 1 тег, 90 дней (~130,000 точек): ~15-20 секунд

**Оптимизации:**
- Downsampling уменьшает количество точек для визуализации
- Isolation Forest использует подвыборки (max_samples=256)
- Корреляции вычисляются только для валидных данных

### Ограничения

**Минимальное количество точек:**
- Для Isolation Forest: минимум 10 точек
- Для классификации: минимум 50 точек

**Максимальное количество тегов:**
- Рекомендуется: до 10 тегов
- Технически: без ограничений (но медленно)

**Поддерживаемые типы данных:**
- Числовые значения (FLOAT, INTEGER)
- Timestamps (TIMESTAMP, DATETIME)

### Обработка ошибок

**Типичные ошибки:**

1. **Недостаточно данных**

- Error: Not enough data points (minimum 10 required)

**Решение:** Увеличить период анализа

2. **Отсутствие данных в периоде**

- Error: No data found for the selected period

**Решение:** Проверить наличие данных в БД

3. **Превышение таймаута**

- Error: Analysis timeout after 60 seconds

**Решение:** Уменьшить период или количество тегов

### Логирование

Модуль использует `structlog` для структурированного логирования.

**Уровни логирования:**
- `DEBUG`: детальная информация о каждом шаге
- `INFO`: основные события (начало/конец анализа)
- `WARNING`: проблемы с данными (пропуски, дубликаты)
- `ERROR`: критические ошибки

**Пример лога:**
```
2026-06-26 10:00:00 [info] Starting DDA analysis tags=["TAG1", "TAG2"] period="7d"
2026-06-26 10:00:02 [info] Data fetched points=10080 tag="TAG1"
2026-06-26 10:00:03 [info] Running Isolation Forest contamination=0.08 n_estimators=100
2026-06-26 10:00:05 [info] Anomalies detected total=42 types={"spike": 15, "dip": 12, "drift": 8, "noise": 7}
2026-06-26 10:00:06 [info] Analysis completed duration_ms=6000
```

---

## Часто задаваемые вопросы (FAQ)

### Q: Почему некоторые аномалии не классифицируются?

**A:** Алгоритм классифицирует аномалии на основе локального контекста. Если аномалия не соответствует критериям spike/dip/drift, она помечается как "noise". Можно настроить порог классификации в конфигураторе.

### Q: Можно ли анализировать более 10 тегов одновременно?

**A:** Технически да, но это будет медленно. Рекомендуется анализировать не более 5-10 тегов за раз. Для больших наборов данных используйте последовательный анализ.

### Q: Почему графики выглядят "сжатыми"?

**A:** Это результат downsampling. Увеличьте параметр "Max Points" в конфигураторе для более детального отображения (но это замедлит рендеринг).

### Q: Как экспортировать результаты?

**A:** Используйте кнопку "Download PNG" в модалке графика (кнопка ⛶ на графике). Также можно сохранить анализ через кнопку "Save" и загрузить его позже.

### Q: Почему корреляция показывает 0?

**A:** Возможные причины:
- Нет перекрывающихся данных между тегами
- Данные слишком зашумлены
- Корреляция действительно отсутствует

Проверьте наличие данных для обоих тегов в выбранном периоде.

## 8. Сезонный анализ (Seasonal Analysis)

**Модуль:** `backend/modules/deep_analysis/analyzers/seasonal.py`  
**Версия:** 3.2.8  
**Обновлено:** 2026-06-29

---

## Содержание

1. Обзор методов
2. Детекция доминирующих периодов
3. Декомпозиция временного ряда
4. Вычисление типичного паттерна
5. Статистические метрики
6. Интерпретация результатов
7. Ограничения и особенности

---

## Обзор методов

Сезонный анализ решает три основные задачи:

1. **Обнаружение периодичности** — находит доминирующие циклы в данных (сутки, неделя, месяц и т.д.)
2. **Декомпозиция ряда** — разделяет временной ряд на тренд, сезонную компоненту и остаток
3. **Извлечение типичного паттерна** — вычисляет усреднённый профиль одного цикла

Все методы работают для **любой частоты дискретизации** (sampling rate) — не зависят от 5-минутного или 1-часового интервала.

---

## Детекция доминирующих периодов

### Метод: FFT + Автокорреляция

Функция `detect_dominant_periods(values, min_period, max_period)` использует два взаимодополняющих метода:

#### Шаг 1: Быстрое преобразование Фурье (FFT)

Для временного ряда $x = [x_0, x_1, \ldots, x_{N-1}]$ длиной $N$ вычисляется FFT:

$$X_k = \sum_{n=0}^{N-1} x_n \cdot e^{-i \cdot 2\pi k n / N}, \quad k = 0, 1, \ldots, N-1$$

где:
- $x_n$ — значение в момент времени $n$
- $X_k$ — комплексная амплитуда $k$-й частоты
- $i$ — мнимая единица

**Спектр мощности (Power Spectrum):**

$$P_k = |X_k|^2 = \text{Re}(X_k)^2 + \text{Im}(X_k)^2$$

Частоты $f_k$ связаны с периодами $T_k$:

$$f_k = \frac{k}{N \cdot \Delta t}, \quad T_k = \frac{N \cdot \Delta t}{k}$$

где $\Delta t$ — интервал между измерениями (например, 5 минут).

**Нормализация спектра:**

$$\hat{P}_k = \frac{P_k}{\sum_{k} P_k}$$

#### Шаг 2: Автокорреляция (ACF)

Для подтверждения периодов используется автокорреляционная функция:

$$R(\tau) = \frac{1}{N-\tau} \sum_{t=0}^{N-\tau-1} (x_t - \bar{x})(x_{t+\tau} - \bar{x})$$

**Нормализованная автокорреляция:**

$$\rho(\tau) = \frac{R(\tau)}{R(0)}$$

где $\rho(0) = 1$, а $\rho(\tau) \in [-1, 1]$.

**Интерпретация:**
- $\rho(\tau) \approx 1$ — сильная положительная корреляция с лагом $\tau$
- $\rho(\tau) \approx -1$ — сильная отрицательная корреляция (анти-период)
- $\rho(\tau) \approx 0$ — нет линейной зависимости

#### Шаг 3: Объединённая оценка (Confidence Score)

Для каждого периода $T$ вычисляется composite confidence:

$$\text{confidence}(T) = w_1 \cdot \hat{P}_{T} + w_2 \cdot \max(\rho(T), 0) + w_3 \cdot \text{peak\_sharpness}(T)$$

где:
- $w_1 = 0.5$ — вес FFT
- $w_2 = 0.3$ — вес автокорреляции
- $w_3 = 0.2$ — вес "остроты пика" (peak sharpness)

**Peak sharpness** измеряет насколько пик выражен относительно соседей:

$$\text{sharpness}(T) = \frac{\hat{P}_T - \text{median}(\hat{P}_{T-2:T+2})}{\text{std}(\hat{P}_{T-2:T+2}) + \epsilon}$$

#### Шаг 4: Ранжирование и отбор

Периоды ранжируются по confidence и возвращаются **top-K** (обычно K=5):

$$T_{\text{ranked}} = \text{argsort}(\text{confidence})[-K:]$$

**Фильтрация:**
- Периоды вне диапазона $[T_{\min}, T_{\max}]$ отбрасываются
- Периоды короче `min_period` точек отбрасываются (шум)
- Периоды длиннее ряда / 2 отбрасываются (недостаточно данных)

### Псевдокод

```python
def detect_dominant_periods(values, min_period=10, max_period=None):
    N = len(values)
    if max_period is None:
        max_period = N // 2
    
    # Удаляем тренд (detrending)
    detrended = values - rolling_mean(values, window=N//4)
    
    # FFT
    fft_vals = np.fft.fft(detrended)
    power_spectrum = np.abs(fft_vals[:N//2])**2
    power_norm = power_spectrum / power_spectrum.sum()
    
    # Автокорреляция
    acf = np.correlate(detrended, detrended, mode='full')[N-1:]
    acf_norm = acf / acf[0]
    
    # Composite confidence
    confidences = {}
    for T in range(min_period, max_period):
        fft_score = power_norm[N//T] if N//T < len(power_norm) else 0
        acf_score = max(acf_norm[T], 0)
        sharpness = compute_sharpness(power_norm, N//T)
        
        confidences[T] = 0.5*fft_score + 0.3*acf_score + 0.2*sharpness
    
    # Top-K periods
    top_periods = sorted(confidences.items(), key=lambda x: x[1], reverse=True)[:5]
    return top_periods
```

---

## Декомпозиция временного ряда

### Метод: Аддитивная STL-подобная декомпозиция

Функция `decompose_seasonal(values, period)` разделяет ряд на три компоненты:

$$x_t = T_t + S_t + R_t$$

где:
- $T_t$ — трендовая компонента (Trend)
- $S_t$ — сезонная компонента (Seasonal)
- $R_t$ — остаточная компонента (Residual)

### Шаг 1: Оценка тренда (Trend)

Используется **скользящее среднее (rolling mean)** с окном, равным периоду:

$$T_t = \frac{1}{2m+1} \sum_{j=-m}^{m} x_{t+j}$$

где $m = \lfloor \text{period}/2 \rfloor$.

**Для чётных периодов** применяется центрированное скользящее среднее 2×m:

$$T_t = \frac{1}{2} \left( \frac{1}{m} \sum_{j=-(m-1)/2}^{(m+1)/2} x_{t+j} + \frac{1}{m} \sum_{j=-(m+1)/2}^{(m-1)/2} x_{t+j} \right)$$

На практике используется **pandas rolling window** с `center=True`:

```python
trend = pd.Series(values).rolling(window=period, center=True, min_periods=1).mean()
```

**Улучшенный алгоритм:** для уменьшения edge effects применяется `win_type='gaussian'` или LOESS-сглаживание.

### Шаг 2: Detrending

Вычитаем тренд из исходного ряда:

$$D_t = x_t - T_t$$

где $D_t$ — детрендированный ряд.

### Шаг 3: Вычисление сезонной компоненты (Seasonal)

Сезонная компонента вычисляется как **среднее по фазам**:

$$S_p = \frac{1}{K_p} \sum_{k=0}^{K_p-1} D_{p + k \cdot \text{period}}$$

где:
- $p \in [0, \text{period}-1]$ — индекс фазы
- $K_p$ — количество наблюдений в фазе $p$
- $D_{p + k \cdot \text{period}}$ — детрендированные значения в фазе $p$

**Центрирование сезонности** (чтобы сумма по периоду была ≈ 0):

$$\bar{S} = \frac{1}{\text{period}} \sum_{p=0}^{\text{period}-1} S_p$$
$$S'_p = S_p - \bar{S}$$

**Расширение на весь ряд:**

$$S_t = S'_{t \bmod \text{period}}$$

### Шаг 4: Вычисление остатка (Residual)

$$R_t = x_t - T_t - S_t = D_t - S'_t$$

### Variance Explained (Доля объяснённой дисперсии)

Каждая компонента оценивается через долю объяснённой дисперсии:

$$\text{Var}(X) = \frac{1}{N} \sum_{t=0}^{N-1} (x_t - \bar{x})^2$$

**Доля тренда:**

$$\text{Var}_{\text{trend}} = \frac{\text{Var}(T)}{\text{Var}(X)} \cdot 100\%$$

**Доля сезонности:**

$$\text{Var}_{\text{seasonal}} = \frac{\text{Var}(S)}{\text{Var}(X)} \cdot 100\%$$

**Доля остатка:**

$$\text{Var}_{\text{residual}} = \frac{\text{Var}(R)}{\text{Var}(X)} \cdot 100\%$$

**Проверка:** $\text{Var}_{\text{trend}} + \text{Var}_{\text{seasonal}} + \text{Var}_{\text{residual}} \approx 100\%$

### Псевдокод

```python
def decompose_seasonal(values, period):
    N = len(values)
    
    # 1. Trend: rolling mean
    trend = pd.Series(values).rolling(window=period, center=True).mean()
    trend = trend.fillna(method='bfill').fillna(method='ffill')
    
    # 2. Detrending
    detrended = values - trend
    
    # 3. Seasonal: mean by phase
    seasonal_pattern = []
    for phase in range(period):
        phase_values = detrended[phase::period]
        seasonal_pattern.append(np.mean(phase_values))
    
    # Центрирование
    seasonal_pattern = np.array(seasonal_pattern)
    seasonal_pattern -= seasonal_pattern.mean()
    
    # Расширение на весь ряд
    seasonal = np.tile(seasonal_pattern, N // period + 1)[:N]
    
    # 4. Residual
    residual = values - trend - seasonal
    
    # 5. Variance explained
    var_total = np.var(values)
    var_trend = np.var(trend)
    var_seasonal = np.var(seasonal)
    var_residual = np.var(residual)
    
    return {
        "trend": trend.tolist(),
        "seasonal": seasonal.tolist(),
        "residual": residual.tolist(),
        "seasonal_pattern": seasonal_pattern.tolist(),
        "variance_explained": {
            "trend": (var_trend / var_total) * 100,
            "seasonal": (var_seasonal / var_total) * 100,
            "residual": (var_residual / var_total) * 100,
        }
    }
```

---

## Вычисление типичного паттерна

### Метод: Усреднение по фазам

Функция `get_seasonal_pattern(values, period)` возвращает **типичный профиль одного цикла**:

$$\text{pattern}[p] = \frac{1}{K_p} \sum_{k=0}^{K_p-1} x_{p + k \cdot \text{period}}$$

где:
- $p \in [0, \text{period}-1]$ — индекс фазы (0 = начало цикла)
- $K_p = \lfloor (N - p - 1) / \text{period} \rfloor + 1$ — количество наблюдений в фазе $p$
- $x_{p + k \cdot \text{period}}$ — исходные значения (НЕ детрендированные!)

**Важно:** В отличие от декомпозиции, здесь используется **исходный ряд** (не детрендированный), чтобы паттерн сохранял абсолютные значения.

### Стандартное отклонение по фазам

$$\text{std}[p] = \sqrt{\frac{1}{K_p} \sum_{k=0}^{K_p-1} (x_{p + k \cdot \text{period}} - \text{pattern}[p])^2}$$

### Количество наблюдений

$$\text{n\_samples}[p] = K_p$$

### Псевдокод

```python
def get_seasonal_pattern(values, period):
    clean_values = [v for v in values if v is not None and not np.isnan(v)]
    N = len(clean_values)
    
    pattern = []
    stds = []
    counts = []
    
    for phase in range(period):
        # Срез всех значений с данной фазой
        phase_values = clean_values[phase::period]
        
        if len(phase_values) > 0:
            pattern.append(float(np.mean(phase_values)))
            stds.append(float(np.std(phase_values)))
            counts.append(len(phase_values))
        else:
            pattern.append(None)
            stds.append(None)
            counts.append(0)
    
    return {
        "pattern": pattern,      # Среднее по каждой фазе
        "std": stds,             # Стандартное отклонение по каждой фазе
        "n_samples": counts,     # Количество наблюдений
        "period": period,        # Период
    }
```

### Пример: 7-дневный ряд с 5-минутным интервалом

- Длина ряда: $N = 2016$ точек
- Период: $T = 288$ точек (24 часа)
- Количество полных циклов: $K = 2016 / 288 = 7$ (7 суток)

Для фазы $p = 0$ (полночь):
- Точки: $x_0, x_{288}, x_{576}, x_{864}, x_{1152}, x_{1440}, x_{1728}$
- Всего: 7 значений
- $\text{pattern}[0] = \frac{x_0 + x_{288} + x_{576} + \ldots + x_{1728}}{7}$

**Результат:** Паттерн из 288 точек, где каждая точка — это **усреднённое значение за все 7 дней** в данный момент суток.

---

## Статистические метрики

### Confidence Score (Уверенность детекции)

Метрика показывает, насколько **надёжно** обнаружен период:

$$\text{confidence} = \frac{\text{composite\_score} - \text{median}}{\text{std}} \cdot 100\%$$

**Интерпретация:**
- **> 50%** — очень надёжный период (чёткие суточные/недельные циклы)
- **20-50%** — умеренная уверенность (зашумлённые данные)
- **< 20%** — слабая периодичность (возможно, ложное обнаружение)

### Минимум, максимум, размах паттерна

$$\text{min} = \min_{p} \text{pattern}[p]$$
$$\text{max} = \max_{p} \text{pattern}[p]$$
$$\text{range} = \text{max} - \text{min}$$

### Амплитуда сезонности

**Абсолютная:**

$$A_{\text{abs}} = \frac{\text{range}}{2}$$

**Относительная (в % от среднего):**

$$A_{\text{rel}} = \frac{\text{range}}{\text{mean}(\text{pattern})} \cdot 100\%$$

---

## Интерпретация результатов

### Пример: KITCHEN2-CO2 (7 дней, 5-мин интервал)

#### Входные данные:
- Длина ряда: **8581 точек**
- Период: **30 дней**
- Sampling rate: **5 минут** (288 точек/сутки)

#### Результат анализа:

```json
{
  "periods": {
    "detected_periods": [
      {"period": 2016, "confidence": 0.87},   // ~7 дней (неделя)
      {"period": 288, "confidence": 0.72},    // ~24 часа (сутки)
      {"period": 1440, "confidence": 0.45},   // ~5 дней
      {"period": 576, "confidence": 0.32},    // ~2 дня
      {"period": 864, "confidence": 0.18}     // ~3 дня
    ]
  },
  "decomposition": {
    "variance_explained": {
      "trend": 1.5,
      "seasonal": 87.1,
      "residual": 11.2
    }
  },
  "pattern": {
    "period": 288,
    "pattern": [498.1, 493.4, 490.3, ..., 481.0],
    "min": 426.0,
    "max": 642.7,
    "range": 216.7
  }
}
```

#### Интерпретация:

**1. Доминирующие периоды:**
- **288 точек (~24ч)** — основной суточный цикл с уверенностью 72%
- **2016 точек (~7 дней)** — недельный цикл с уверенностью 87% (доминирующий!)

**2. Декомпозиция:**
- **Тренд: 1.5%** — почти нет долгосрочного изменения
- **Сезонность: 87.1%** — подавляющая часть изменчивости объясняется циклами
- **Остаток: 11.2%** — шум и несистематические колебания

**Вывод:** Ряд **высоко периодичен** (87% сезонности) с сильными суточными и недельными циклами.

**3. Типичный паттерн:**
- **Мин: 426 ppm** — ночной минимум CO₂ (низкая активность)
- **Макс: 643 ppm** — дневной пик (рабочие часы на кухне)
- **Размах: 217 ppm** — амплитуда суточных колебаний

### Что означает высокая сезонность?

| Var_seasonal | Интерпретация | Примеры |
|--------------|---------------|---------|
| **> 70%** | Очень периодичный ряд | Температура в здании, нагрузка на энергосистему |
| **40-70%** | Умеренная периодичность | Трафик, потребление воды |
| **< 40%** | Слабая периодичность | Случайные события, шумовые данные |

### Что означает высокий residual?

| Var_residual | Интерпретация | Возможные причины |
|--------------|---------------|-------------------|
| **< 20%** | Чистые данные | Стабильный процесс |
| **20-40%** | Умеренный шум | Нормальная вариативность |
| **> 40%** | Сильный шум | Аномалии, нестабильность, ошибки измерения |

---

## Ограничения и особенности

### 1. Длина ряда

**Минимум:** 50 точек  
**Рекомендуется:** ≥ 3 полных периода

Для обнаружения периода $T$ необходимо минимум $3T$ точек в ряде.

### 2. Пропущенные значения

- **Метод:** `_prepare_values(interpolate=False)` удаляет NaN
- **Последствия:** сокращение эффективной длины ряда
- **Решение:** для больших gap'ов рекомендуется интерполяция перед анализом

### 3. Нестационарность

Методы предполагают, что сезонность **стабильна во времени**. Если паттерн меняется (например, зимний/летний режимы), результаты будут **усреднёнными** и могут не отражать реальность.

**Решение:** использовать скользящее окно или separate analysis по сегментам.

### 4. Edge effects при декомпозиции

**Проблема:** Rolling mean на краях ряда даёт менее точные оценки тренда.

**Решение:** `fillna(method='bfill').fillna(method='ffill')` продлевает тренд, но первые и последние `period/2` точек имеют повышенную неопределённость.

### 5. Кратные периоды

Если ряд имеет **несколько наложенных циклов** (сутки + неделя), FFT покажет оба. Метод возвращает **top-5** периодов, и важно анализировать их вместе.

**Пример:** Период 2016 точек (неделя) часто сопровождается 288 точек (сутки) — это **нормально**, а не дублирование.

### 6. Выбор периода для анализа

**Рекомендация:** использовать **самый короткий устойчивый период** (обычно сутки).

**Обоснование:** Короткий период даёт больше циклов для усреднения → более гладкий паттерн.

```python
# Выбор периода
main_period = periods['detected_periods'][0]['period']  # Самый уверенный

# Но если он слишком длинный — берём следующий
if main_period > 1000 and len(periods['detected_periods']) > 1:
    main_period = periods['detected_periods'][1]['period']
```

---

## Сводная таблица методов

| Метод | Формула | Назначение |
|-------|---------|------------|
| **FFT** | $X_k = \sum x_n e^{-i2\pi kn/N}$ | Частотный анализ |
| **Power Spectrum** | $P_k = \|X_k\|^2$ | Мощность по частотам |
| **ACF** | $\rho(\tau) = R(\tau)/R(0)$ | Автокорреляция |
| **Trend** | $T_t = \text{mean}(x_{t-m:t+m})$ | Скользящее среднее |
| **Seasonal** | $S_p = \text{mean}_k(D_{p+kT})$ | Среднее по фазам |
| **Residual** | $R_t = x_t - T_t - S_t$ | Остаток |
| **Variance Explained** | $\text{Var}(S)/\text{Var}(X)$ | Доля дисперсии |
| **Pattern** | $\text{mean}_k(x_{p+kT})$ | Типичный профиль |
| **Confidence** | $w_1 \hat{P} + w_2 \rho + w_3 \text{sharpness}$ | Уверенность |

---

## Ссылки

1. **Box, G.E.P., Jenkins, G.M., Reinsel, G.C.** (2013). *Time Series Analysis: Forecasting and Control*. Wiley.
2. **Cleveland, R.B., Cleveland, W.S., McRae, J.E., Terpenning, I.** (1990). *STL: A Seasonal-Trend Decomposition Procedure Based on Loess*. Journal of Official Statistics.
3. **Chatfield, C.** (2003). *The Analysis of Time Series: An Introduction*. Chapman & Hall/CRC.

---

## FAQ

### Q: Почему для периода 2016 точек (7 дней) паттерн показывает "волны", а не прямую линию?

**A:** Потому что в данных есть **реальные суточные колебания** (минимум ночью, пик днём). Алгоритм `get_seasonal_pattern` усредняет 7 суток и выявляет эти колебания. Если бы ряд был случайным шумом, паттерн был бы плоским.

### Q: Как понять, что детектированный период реальный, а не артефакт?

**A:** Смотрите на **confidence score**:
- **> 50%** — реальный период
- **20-50%** — вероятно реальный, но зашумлённый
- **< 20%** — возможно, артефакт

Также проверяйте **Var_seasonal** в декомпозиции — если сезонность > 50%, периоды скорее всего реальные.

### Q: Почему seasonal анализ выполняется на всём ряде, а не на отдельных сегментах?

**A:** Это **осознанный выбор**: усреднение по всем циклам даёт **наиболее стабильный и сглаженный** типичный паттерн. Для анализа изменяющейся во времени сезонности потребуется отдельный метод (например, скользящее окно).

### Q: Можно ли использовать для рядов с разной частотой дискретизации?

**A:** Да, алгоритмы **не зависят от sampling rate**. Период 288 точек может быть:
- 24 часа при 5-мин интервале
- 12 часов при 2.5-мин интервале
- 48 часов при 10-мин интервале

Формат `formatPeriod(period)` автоматически определяет, какой это период в часах/днях, исходя из фактической частоты.

### Q: Что делать, если ряд имеет тренд?

**A:** Декомпозиция **автоматически** разделяет тренд и сезонность. Но для `get_seasonal_pattern` используется **исходный ряд** (не детрендированный), поэтому паттерн сохраняет абсолютные значения.

Если нужен "чистый" сезонный паттерн без тренда, используйте `decomposition.seasonal_pattern` вместо `pattern.pattern`.

---